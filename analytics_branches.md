# 有序旅程、六组分岔与下载门槛分析

`analytics_branches.py` 提供纯 Python 接口：

```python
result = analyze_branches(visits, events, observation_start, observation_end)
```

输入使用共享提取 schema。模块不访问数据库；主要需求分类优先读取 `visits.initial_demand`，仅缺失时才按路径映射。

## 统一口径

- 主人群要求 `utm_source=meigen/meigen.ai`，或 `referrer_domain` 等于 `meigen.ai` / 它的子域；不接受名称仅含该字符串的外域。
- 事件按时间排序，用 event_id 作确定性排序键。重复 event_id 移除，但**不**借 UUID 推断同时间事件先后。首次消息同时间存在多个记录时，重复使用要求严格更晚的一次提交。
- 页面浏览只认 Umami `event_type=1` 或明确 page-view 名称。携带 `url_path` 的自定义事件不是 PV。
- 每节点成熟分母要求 `anchor+30min <= frozen_end`。节点结果取严格更晚、30分钟内首个目标，故 `使用→注册→再次使用` 能正确计入注册后续接。缺乏30分钟观测的起点单独报告。
- `continue_rate` 是观察到目标动作的比例；`no_target_rate` 是未到目标动作的比例。`exit_rate` 是无后续有效动作的比例，排除仅 `login`、自动侧栏曝光等被动遥测。`no_later_record` 另外给出完全没有后续原始记录的规模，`other_later_activity` 给出有记录但未到目标的规模。
- 没有 MeiGen 站外曝光/点击数据。入口和落地是同一个站内可观测起点，二者都查询后续主动进展，**不**给外链点击到达率或100%转化结论。
- 所有比较按自然周（北京时间、周一开始）分层；全历史总体保留用于规模，但历史事件覆盖变化仍须与主数据覆盖表共同解释。

## 流程轴

展示顺序是 `entry → landing → browse → register → use → reuse → payment`，每个节点保留明确的实际后继目标：

| 节点 | 观测定义 | 点开后主要推进指标 |
|---|---|---|
| 入口/落地 | 站内来源访问与首个PV | 浏览其他页面或主动操作 |
| 浏览 | 后续不同非认证页面、点击或选择 | 继续注册或提交任务，允许跳过注册 |
| 注册 | `sign_up` / `guest_signup` | 注册后提交任务 |
| 使用 | 首次 `send_message` | 严格更晚的再次提交 |
| 复用 | 第二个不同时间的提交 | 进入付费意图 |
| 支付 | `purchase`信号 | 支付后继续提交 |

`axis` 同时输出实际观测下一事件分布与按需求、入口×设备×周分组的节点统计；前端可通过这些分支表达实际顺序，避免强制单线。

## 六组核心分岔

`branches` 中包含：

1. `entry_continue`：进入之后继续操作 vs 停止。
2. `browse_use`：浏览后提交任务 vs 未提交。
3. `attempt_continue`：首次尝试后再操作/进入付费路径 vs 停止；作为缺失结果完成信号的替代分析。
4. `use_register`：当次首次尝试前未见注册的人，之后注册 vs 未见注册；下载门槛是更直接的补充场景。
5. `signup_use`：注册后继续提交 vs 未继续。
6. `use_commercial`：使用后进入付费路径 vs 未进入，使用之前已见支付者排除。

注册/付费的首次转化分支也会排除无冲突账户在已提供历史中更早发生的注册/支付；未知历史不由 login 推断。

每组给成熟分母、继续/停止人数、率、Wilson区间、耗时中位数/P90、同需求分层、入口×设备×周分层、前置特征分布和比较。前置特征严格取锚点之前，只有首次提交的输入附件属性属于锚点同时输入。

对照输出两部分：原始比例差及 Newcombe 区间；共同覆盖需求×入口×周×设备单元的加权比例差。没有共同覆盖则不输出加权值。卡片 `fix-blurry` 两版本另给明确对照。加权消除可见构成差异，但不被描述为因果效果。

## 认证、下载与价值代理

- `auth_resume`：从认证PV开始，追踪身份恢复、返回页面、返回原路径、返回后提交和认证后直接提交。`login`不作为主动登录/注册。没有 task_id 时，不把回到相同页面或提交断言成同一个任务恢复。
- `download_gate`：`guest_gate_shown.reason=download`、`paywall_shown.scene=download`的首次曝光。按 Visit×事件×场景×原因去重，给之后注册、使用、价格、意图、结账、purchase与注册后继续。下载与其他场景比较时增加前置注册、使用次数分层。
- `guest_signup.reason=download`用于下载门槛后的注册结果；它自身不是门槛曝光分母。
- `early_repeat_to_intent`：首次使用后2/5分钟冻结“重复使用”特征；窗口结束后至首次使用+30分钟观察新付费意图，窗口内已有付费意图者单列。该分析不要求之后仍活跃，避免人为剔除停止使用者。
- `commercial_path`：独立保存价格、选套餐/权益点击、结账、支付到达量，有序商业推进、结账动作类型和回流方式。
- `paths`：实际路径、相邻转换与每组继续/中断的典型路径。代表样本ID哈希后展示，保留相对节点时间，不输出账号标识。

`send_message`表示前端接受的提交信号，不是产出完成。价格浏览、付费意图、结账和purchase分别保存，不把结账意图说成已付款。缺失的生成/下载完成字段不填成零。

## 验证

2026-09-16 已通过 `py_compile` 与合成时序验证，覆盖：

- 使用→注册→使用的后续匹配；
- PV/custom事件、auth页面与login恢复的区分；
- 同时间重复发送不算复用；
- 有被动记录但无有效动作仍属于退出；
- 30分钟不足访问被保留但排除率分母；
- 严格来源域名与event_id去重；
- 下载门槛去重和其后注册/价格/结账；
- purchase与intent不混用；
- 节点与分岔人数分区守恒。

实际数据运行后还需检查事件覆盖、样本规模及率方向，再选择最值得展开的3项诊断。

## 全历史实际运行补充（2026-09-16）

- 实际输入 34,363 Visit；函数内部排除4个 `quality_eligible=false`，保留34,359 Visit、83,046事件；已采用 `[start,end)` 截止。
- 提交后会自动出现 `/studio/task` 与 `/studio/task/<id>` 的路由PV。这些在使用节点之后不计独立有效后续动作，但保留在原始记录、实际路径与 `no_later_record` 统计中。未做此修正时提交后的自动路由会掩盖首次使用中断。
- `analytics_breakpoint.py` 由 `analyze_branches` 调用，固定复算9/3–9/9（7天）与9/10–9/15（6天）的历史专题，保留同需求×入口×卡片、回跳页面、注册后提交与前5分钟重复使用等证据。它是已审核的固定专题，不自动宣称每次刷新发现新变化。
- `data/branches.json` 保存全部结果，`branches_findings.md` 为业务解释；输入更新时调用同一个函数即可复算所有专题。
- 真实数据独立重算注册→提交2,422/2,741、首次→再次提交652/2,696，与主计算一致；所有节点/分岔及需求分层分母守恒。
